import React from "react";

type LoginProps = {} & React.DetailedHTMLProps<
  React.FormHTMLAttributes<HTMLFormElement>,
  HTMLFormElement
>;

const Login: React.FC<LoginProps> = ({}) => {
  return <div>index</div>;
};

export default Login;
